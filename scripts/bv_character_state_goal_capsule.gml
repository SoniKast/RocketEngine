/// bv_character_state_goal_capsule()
 // Script for the player's goal state after hitting a capsule
 
/*    if(state == CS_GOAL_CAPSULE)
    {
        x_speed = 2;
        lock_timer = 5;
        if(lock_timer = 0)
        {
            animation_index = "GOAL CAPSULE";
            x_speed = 0;   
        }
    }
    



